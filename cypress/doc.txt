-> impilict assertions 
    they are built in
    should
    and 
    -> Parameters
            eq
            contain
            include
            exist
            have.length
            have.value
            be.visible
-> Explicit assertions

    expect - BDD (behavioural driven development) approach
    assert - TDD (test driven development) approach
